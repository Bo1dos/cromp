package com.cromp.orchestrator.workflow;

/**
 * Заглушка для будущего модуля оркестрации сложных рабочих процессов.
 *
 * TODO: реализовать поддержку многошаговых Workflow, когда появится требование\желание.
 *
 * Планируемый функционал:
 * - Цепочки задач (Job A → Job B → Job C) с передачей результатов между шагами.
 * - Условные ветвления (if/else на основе результата предыдущего шага).
 * - Параллельное выполнение независимых шагов с барьером синхронизации.
 * - Компенсирующие транзакции (Saga-паттерн) при частичном сбое.
 *
 * Пока весь функционал покрывается простым Scheduler → Executor → Janitor.
 */
public final class WorkflowStub {

    private WorkflowStub() {
        // Utility-класс, не инстанциируется
    }
}